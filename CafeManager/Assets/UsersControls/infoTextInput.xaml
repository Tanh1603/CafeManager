<UserControl x:Class="CafeManager.WPF.Assets.UsersControls.infoTextInput"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CafeManager.WPF.Assets.UsersControls"
             mc:Ignorable="d" 
              Name="InfoTextInput">
    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="boolToVis"/>
    </UserControl.Resources>
    <Grid>
        <TextBlock Text="{Binding ElementName=InfoTextInput, Path=Hint}" Foreground="#99703a"  Margin="14 0" VerticalAlignment="Center" Panel.ZIndex="1" IsHitTestVisible="False"
               Visibility="{Binding ElementName=textBox, Path=Text.IsEmpty, Converter={StaticResource boolToVis}}"/>
        <TextBox x:Name="textBox" Style="{StaticResource textbox}"/>
    </Grid>
</UserControl>
