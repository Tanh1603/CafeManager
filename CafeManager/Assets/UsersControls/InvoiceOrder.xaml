<UserControl x:Class="CafeManager.WPF.Assets.UsersControls.InvoiceOrder"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CafeManager.WPF.Assets.UsersControls"
             mc:Ignorable="d" 
             Name="invoiceorder"
             Width="225">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="160"/>
            <RowDefinition Height="*"/>
            
        </Grid.RowDefinitions>
        <Grid Grid.Row="0">
            <Border>
                <Border.Background>
                    <ImageBrush ImageSource="/Assets/Images/invoice.png"
                                RenderOptions.BitmapScalingMode="HighQuality"
                                Stretch="Uniform"/>
                </Border.Background>
            </Border>
        </Grid>
        <TextBlock Grid.Row="1" Text="{Binding ElementName=invoiceorder, Path=Text}" HorizontalAlignment="Center" Margin="10" FontSize="18" FontWeight="Bold"  Foreground="#612e0f"/>
    </Grid>
</UserControl>
