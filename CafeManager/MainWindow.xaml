<Window
    x:Class="CafeManager.WPF.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CafeManager.WPF"
    xmlns:uc="clr-namespace:CafeManager.WPF.Views"
    xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
    Title="MainWindow"
    WindowStartupLocation="CenterScreen"
    WindowStyle="none"
    Background="Transparent"
    AllowsTransparency="True"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Background="White">
            <StackPanel Orientation="Horizontal"  HorizontalAlignment="Right">
                <Button Background="Transparent" BorderThickness="0"  Command="{Binding MiniMizeWindownCommand}">
                    <materialDesign:PackIcon Kind="WindowMinimize" Foreground="Black" />
                </Button>
                <Button Background="Transparent" BorderThickness="0" Command="{Binding MaximizeWindownCommand}">
                    <materialDesign:PackIcon Kind="WindowMaximize" Foreground="Black" />
                </Button>
                <Button Background="OrangeRed" Command="{Binding CloseWindownCommand}">
                    <materialDesign:PackIcon Kind="Close" />
                </Button>
            </StackPanel>
        </Grid>
        <Grid Grid.Row="1">
            <ContentControl Content="{Binding CurrentViewModel}" />
        </Grid>
    </Grid>
</Window>