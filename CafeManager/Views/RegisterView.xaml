<UserControl
    x:Class="CafeManager.WPF.Views.RegisterView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CafeManager.WPF.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            FontSize="50"
            Foreground="Black"
            Text="Register" />

        <StackPanel
            Grid.Row="1"
            Margin="20"
            HorizontalAlignment="Center"
            Orientation="Horizontal">
            <Button Margin="0,0,20,0" Content="Quay về đăng nhập" Command="{Binding RegisterCommand}" />
            <Button Content="Đăng Kí" Command="{Binding NavigateToLoginCommand}" />
        </StackPanel>
    </Grid>
</UserControl>