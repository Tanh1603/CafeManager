<UserControl x:Class="CafeManager.WPF.Assets.UsersControls.Order"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:fa="http://schemas.awesome.incremented/wpf/xaml/fontawesome.sharp"
             mc:Ignorable="d" 
             Name="order">
    <Button Style="{StaticResource orderButton}">
        
        <StackPanel Orientation="Horizontal">
            <Grid Width="45" Height="45" Margin="25 0 0 0">
                <Border CornerRadius="10" Style="{StaticResource orderBorderIcon}"/>
                <fa:IconImage Icon="{Binding ElementName=order, Path=Icon}" Style="{StaticResource orderButtonIcon}" />

            </Grid>




            <StackPanel Margin="10 0 0 0" VerticalAlignment="Center">
                <TextBlock Text="{Binding ElementName=order, Path=Title}" FontSize="14"  Style="{StaticResource orderText}"/>
                <TextBlock Text="{Binding ElementName=order, Path=Desc}" FontSize="11"  Margin="0 3 0 0" Style="{StaticResource orderText}"/>
            </StackPanel>
        </StackPanel>
    </Button>
</UserControl>
