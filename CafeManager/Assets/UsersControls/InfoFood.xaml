<UserControl
    x:Class="CafeManager.WPF.Assets.UsersControls.InfoFood"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:CafeManager.WPF.Assets.UsersControls"
    xmlns:ma="http://metro.mahapps.com/winfx/xaml/iconpacks"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Name="infofood"
    Width="225"
    Height="185"
    mc:Ignorable="d">
    <UserControl.Resources>
        <local:Utf8Converter x:Key="Utf8Converter" />
        <local:StringToImageSourceConverter x:Key="StringToImageSourceConverter" />
    </UserControl.Resources>
    <Border
        Margin="0,10,10,0"
        BorderThickness="3"
        CornerRadius="5">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0, 0.5">
                <GradientStop Offset="0" Color="#c69a5d" />
                <GradientStop Offset="0.5" Color="#cb925d" />

                <GradientStop Offset="1" Color="#d4b096" />
            </LinearGradientBrush>
        </Border.BorderBrush>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="130" />
                <RowDefinition Height="*" />

            </Grid.RowDefinitions>


            <Image RenderOptions.BitmapScalingMode="HighQuality" Source="{Binding ElementName=infofood, Path=SourceImage, Converter={StaticResource StringToImageSourceConverter}, FallbackValue='/Assets/Images/defaultImage'}" />


            <Border Grid.Row="1" CornerRadius="0 0 3 3">
                <Border.Style>
                    <Style TargetType="Border">
                        <Setter Property="Background">
                            <Setter.Value>
                                <LinearGradientBrush StartPoint="0,0.5" EndPoint="1,0.5">
                                    <GradientStop Offset="0" Color="#d5b096" />
                                    <GradientStop Offset="1" Color="#d9c6b3" />
                                </LinearGradientBrush>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Border.Style>
                <Grid>

                    <TextBlock
                        Margin="0,0,0,20"
                        FontSize="18"
                        Style="{StaticResource infoFoodText}"
                        Text="{Binding ElementName=infofood, Path=Text, Converter={StaticResource Utf8Converter}}" />

                    <TextBlock
                        Margin="0,20,0,0"
                        Style="{StaticResource infoFoodText}"
                        Text="{Binding ElementName=infofood, Path=Price}" />

                    <ma:PackIconMaterial Kind="TrashCan" Style="{StaticResource infoFoodIcon}" />
                    <ma:PackIconMaterial
                        Margin="0,0,25,0"
                        Kind="Pencil"
                        Style="{StaticResource infoFoodIcon}" />

                </Grid>
            </Border>



        </Grid>
    </Border>
</UserControl>
